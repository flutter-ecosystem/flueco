name: flueco_auth_basic
description: "Flueco Authentication package"
version: 0.0.1-alpha

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flueco_core: 0.0.1-alpha
  flueco_auth: 0.0.1-alpha

dev_dependencies:
  lints: ^3.0.0
  test: ^1.21.0
