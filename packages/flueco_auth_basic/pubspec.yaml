name: flueco_auth_basic
description: "Flueco Authentication package with basic authentication"
version: 0.0.1-alpha.4
repository: https://github.com/flutter-ecosystem/flueco

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flueco_core: ^0.0.1-alpha.5
  flueco_auth: ^0.0.1-alpha.4

dev_dependencies:
  lints: ^5.1.1
  test: ^1.21.0
